<h1>Tasks#show</h1>
<div class="container">
  <%= @task.title %>
  <%= @task.description %>
  <%= @task.created_at %>
  <%= @task.status ? "done" : "to be completed" %>
  <%= link_to "back to list", tasks_path %>
</div>
